# --- 色検出範囲 (HSV) ---
# Note: OpenCVのHSV範囲は H: 0-179, S: 0-255, V: 0-255
COLOR_RANGES = {
    "yellow": ((20, 100, 150), (40, 255, 255)),  # 黄色範囲 (チーム中央マーカー)
    "green": ((40, 30, 100), (70, 255, 255)),   # 緑色範囲 (ロボット前方/後方マーカー)
    "blue": ((90, 80, 100), (130, 255, 255)),   # 青色範囲 (チーム中央マーカー)
    "orange": ((5, 150, 150), (20, 255, 255)),   # オレンジ色範囲 (ボール)
    "magenta": ((140, 30, 80), (170, 255, 255)),  # マゼンタ/ピンク色範囲 (ロボット前方/後方マーカー)
    # この値は実際のカメラと照明条件に合わせて調整してください
}

# --- UDP 通信設定 ---
UDP_IP = "192.168.2.100"  # 制御プログラム(受信側)のIPアドレス
UDP_PORT = 50007          # 使用するポート番号 (制御プログラムと合わせる)

# --- キャリブレーション設定 ---
# キャリブレーションで使用する2点間の実際の距離 (m) (CM -> M, 50 -> 0.5)
REAL_CALIBRATION_DISTANCE_M = 0.5

# --- 検出パラメータ ---
ROBOT_MARKER_MIN_AREA = 2   # ロボットの各マーカーとして認識する最小面積 (ピクセル) - 新しいマーカーに合わせて調整
ORANGE_BALL_MIN_AREA = 2     # オレンジボールとして認識する最小面積 (ピクセル)
BALL_COMPENSATION_TIME = 1.0  # ボールの座標を維持する時間（秒）

# --- 新しい5点マーカーロボット検出用パラメータ ---
MAX_DIST_MIDDLE_TO_OUTER_MARKER = 100  # 中央マーカーから外周マーカーまでの最大許容距離 (ピクセル)
# 前方ペアと後方ペアの距離差の最小値 (ピクセル) - これより差が小さいと前後不明瞭
MIN_PAIR_DISTANCE_DIFFERENCE = 0
# ペアを構成するマーカー間の最小距離 (ピクセル) - これより近いと同じマーカーの誤認識の可能性
MIN_MARKERS_SEPARATION_IN_PAIR = 0

# --- 表示設定 ---
WINDOW_NAME = "Detection Results"  # 表示ウィンドウのタイトル
MASK_TOGGLE_KEY = ord('m')      # マスク表示/通常表示を切り替えるキー
DEBUG_TOGGLE_KEY = ord('d')      # マスク表示/通常表示を切り替えるキー
