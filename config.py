# --- 色検出範囲 (HSV) ---
# Note: OpenCVのHSV範囲は H: 0-179, S: 0-255, V: 0-255
COLOR_RANGES = {
    "red": ((0, 90, 80), (10, 255, 255)),       # 赤色範囲1
    "red2": ((170, 90, 80), (180, 255, 255)),   # 赤色範囲2 (HSVのラップアラウンド対応)
    "yellow": ((20, 100, 150), (40, 255, 255)),  # 黄色範囲
    "green": ((40, 40, 100), (80, 255, 255)),   # 緑色範囲
    "blue": ((90, 80, 100), (130, 255, 255)),   # 青色範囲
    "orange": ((5, 150, 150), (20, 255, 255)),   # オレンジ色範囲
}

# --- UDP 通信設定 ---
UDP_IP = "192.168.50.86"  # 制御プログラム(受信側)のIPアドレス
UDP_PORT = 50007          # 使用するポート番号 (制御プログラムと合わせる)

# --- キャリブレーション設定 ---
REAL_CALIBRATION_DISTANCE_CM = 50  # キャリブレーションで使用する2点間の実際の距離 (cm)

# --- 検出パラメータ ---
ROBOT_MARKER_MIN_AREA = 800    # ロボットのマーカー(赤/緑/黄/青)として認識する最小面積 (ピクセル)
ORANGE_BALL_MIN_AREA = 100     # オレンジボールとして認識する最小面積 (ピクセル)
MIN_ARROW_LENGTH_PIXELS = 50   # ロボット検出時に赤と緑のマーカーが離れているべき最小距離 (ピクセル)

# --- 表示設定 ---
WINDOW_NAME = "Detection Results"  # 表示ウィンドウのタイトル
MASK_TOGGLE_KEY = ord('m')      # マスク表示/通常表示を切り替えるキー
